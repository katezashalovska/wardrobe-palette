services:
  - type: static
    name: ai-wardrobe-organizer
    env: static
    buildCommand: npm ci && npm run build -- --verbose
    publishDir: dist
    envVars:
      - key: NODE_VERSION
        value: 22
