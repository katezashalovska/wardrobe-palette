services:
  - type: static
    name: ai-wardrobe-organizer
    env: static
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: NODE_VERSION
        value: 22
