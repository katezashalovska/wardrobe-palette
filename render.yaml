services:
  - type: static
    name: ai-wardrobe-organizer
    env: static
    buildCommand: npm ci && npm run build -- --verbose
    publishDir: dist
    envVars:
      - key: NODE_VERSION
        value: 22
    routes:
      - type: rewrite
        source: /privacy
        destination: /privacy.html
